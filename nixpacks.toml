[phases.setup]
nixPkgs = ["nodejs_22"]
[phases.install]
cmds = ["npm ci"]
[phases.build]
cmds = ["npm run build"]
[start]
cmd = "node .next/standalone/server.js"
[env]
NODE_ENV = "production"
PORT = "3000"
