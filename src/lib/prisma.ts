// Legacy — prisma is no longer used. All data access now goes through Mongoose models.
// This file exists only to prevent import errors from any remaining references.
// TODO: Remove this file and update all remaining imports.

export const prisma = null;
